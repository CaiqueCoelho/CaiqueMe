<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="caique.me.caiqueme">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutMeActivity" />
        <activity android:name=".TimelineActivity" />
        <activity android:name=".DescJobActivity" />
        <activity android:name=".ProjectsActivity" />
        <activity android:name=".DescProjectActivity" />
        <activity android:name=".WebviewActivity" />
        <activity android:name=".AboutAppActivity" />
        <activity android:name=".MatchActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".TchauActivity"></activity>
    </application>

</manifest>